{
  "name": "@webapp/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "npm run test:unit && npm run test:api",
    "test:unit": "vitest run test/unit",
    "test:api": "vitest run test/api",
    "test:integration": "RUN_DB_TESTS=1 vitest run test/db"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/pg": "^8.11.11",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
